{
  "name": "wavlang",
  "version": "1.0.0",
  "description": "Transcription Service",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install --legacy-peer-deps",
    "install:all": "npm run install:backend && npm run install:frontend",
    "start:backend": "cd backend && npm install && node server.js",
    "start:frontend": "cd frontend && npm install --legacy-peer-deps && npm run build && npm start",
    "build:frontend": "cd frontend && npm run build",
    "start": "cd backend && npm install && node server.js",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\""
  },
  "dependencies": {
    "js-cookie": "^3.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
